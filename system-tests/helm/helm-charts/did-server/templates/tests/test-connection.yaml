apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "did-server.fullname" . }}-test-connection"
  labels:
    {{- include "did-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "did-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
