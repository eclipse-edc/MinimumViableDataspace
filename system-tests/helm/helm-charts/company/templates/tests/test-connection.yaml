apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "company.fullname" . }}-test-connection"
  labels:
    {{- include "company.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "company.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
