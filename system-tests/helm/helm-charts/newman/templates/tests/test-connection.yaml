apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newman.fullname" . }}-test-connection"
  labels:
    {{- include "newman.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newman.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
