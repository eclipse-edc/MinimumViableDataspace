meta {
  name: Get Credential By ID
  type: http
  seq: 1
}

get {
  url: {{CS_URL}}/api/identity/v1alpha/participants/{{PARTICIPANT_CONTEXT_ID}}/credentials/CREDENTIAL-ID
  body: none
  auth: inherit
}

script:post-response {
  test("Status is OK", function() {
    expect(res.getStatus()).to.be.oneOf([200, 204])
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
