meta {
  name: Add endpoint
  type: http
  seq: 4
}

post {
  url: {{CS_URL}}/api/identity/v1alpha/participants/{{PARTICIPANT_CONTEXT_ID}}/dids/{{DID}}/endpoints
  body: json
  auth: inherit
}

body:json {
  {
      "id": "some-other-id",
      "type": "CredentialService",
      "serviceEndpoint": "https://foobar.myconnector.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
