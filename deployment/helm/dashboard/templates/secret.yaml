apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dashboard.fullname" . }}-envvars
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $secretName, $value := .Values.dashboard.envSecrets }}
  {{ $secretName }}: {{ $value | b64enc }}
{{- end }}
