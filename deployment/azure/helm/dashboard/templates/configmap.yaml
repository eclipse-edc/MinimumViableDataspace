apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dashboard.fullname" . }}-envvars
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
data:
  {{- range $envName, $value := .Values.dashboard.env }}
  {{ $envName | quote }}: {{ $value | quote }}
  {{- end }}
