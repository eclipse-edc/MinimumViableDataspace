apiVersion: v1
kind: Service
metadata:
  name: {{ include "participant.fullname" . }}
  labels:
    {{- include "participant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "participant.selectorLabels" . | nindent 4 }}
